function UIDropDownMenu_GetSelectedID(frame)容易受到污染，因为frame.selectedID被set污染了。
UIDROPDOWNMENU_MENU_VALUE -- 这个绝对不能被污染
UIDROPDOWNMENU_MENU_LEVEL 这两个容易被污染

move，点击魔兽好友会触发污染

只要有魔兽好友出现在列表中，切换标签会触发污染

右键不会污染，但左键会。

BottomEdge偶尔会出现，消失的情况

好像会自己保存一次通告

整合两个插件

新功能，决斗谁战胜了谁，添加职业颜色


-- 滚动框体的更新，框体触底是不会更新的。
-- hooksecurefunc("HybridScrollFrame_Update",function ()
-- 	print("滚动更新")
-- end)

-- 当焦点在滚动滑块上的时候
-- 即使触底这个也是会执行的
-- hooksecurefunc("HybridScrollFrame_OnMouseWheel",function ()
-- 	print("滚动child更新")
-- end)

-- 滚动条上下两个三角按钮被按下的时候就会触发。
-- hooksecurefunc("HybridScrollFrameScrollButton_OnUpdate",function ()
-- 	print("HybridScrollFrameScrollButton_OnUpdate触发")
-- end)


-- 标签的切换回导致内存上升
-- 会导致设置焦点被污染
-- 右侧的私聊角色会导致改变。
-- 似乎只要私聊角色就会导致的污染
-- 可能只要右侧的右键就会污染
-- 有三个插件都在尝试调用保护程序

-- 确定# ext_Friends.lua会触发污染
-- friendcolor会导致污染。
-- ext_Who.lua？

-- 在UnitPopup_HideButtons只，可能照成污染的是UIDROPDOWNMENU_INIT_MENU、UnitPopupMenus和UIDROPDOWNMENU_MENU_VALUE和dropdownMenu.which